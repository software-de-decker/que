{
  "name": "que",
  "version": "0.0.1",
  "description": "Entertainment watchlist platform with social features",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:web\"",
    "dev:backend": "npm run start:dev --workspace=apps/backend",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:mobile": "npm start --workspace=apps/mobile",
    "build": "npm run build --workspaces --if-present",
    "build:backend": "npm run build --workspace=apps/backend",
    "build:web": "npm run build --workspace=apps/web",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "npm run format --workspaces --if-present",
    "test:backend": "npm run test --workspace=apps/backend",
    "test:backend:e2e": "npm run test:e2e --workspace=apps/backend",
    "test:web": "npm run test --workspace=apps/web",
    "lint:backend": "npm run lint --workspace=apps/backend",
    "lint:web": "npm run lint --workspace=apps/web",
    "format:backend": "npm run format --workspace=apps/backend",
    "format:web": "npm run format --workspace=apps/web",
    "db:generate": "npm run db:generate --workspace=apss/backend",
    "db:push": "npm run db:push --workspace=apps/backend",
    "db:migrate": "npm run db:migrate --workspace=apps/backend",
    "db:studio": "npm run db:studio --workspace=apps/backend",
    "db:start": "docker-compose up -d",
    "db:stop": "docker-compose stop",
    "db:restart": "docker-compose restart",
    "db:logs": "docker-compose logs -f postgres",
    "db:reset": "docker-compose down -v && docker-compose up -d",
    "db:connect": "docker exec -it que-postgres psql -U que_user -d que_dev"
  },
  "devDependencies": {
    "concurrently": "8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
